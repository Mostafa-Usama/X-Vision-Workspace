<UserControl x:Class="Center_Maneger.UesrControls.Member_Offers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource bguc}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.35*"/>
            <RowDefinition Height="0.65*"/>
        </Grid.RowDefinitions>

        
            <StackPanel Grid.Row="0">

            <Border Width="150" BorderBrush="{StaticResource bgtab}" Background="{StaticResource bguc}" CornerRadius="7" BorderThickness="5">
                <TextBlock Text="عروض الاعضاء" FontSize="18" FontWeight="Bold" Foreground="{StaticResource bgtab}" HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <StackPanel Margin="0 25" Orientation="Horizontal" HorizontalAlignment="Center" FlowDirection="RightToLeft">
            <CheckBox FlowDirection="RightToLeft" Content="العروض المنتهية " x:Name="expired_offers" FontSize="22" FontWeight="Bold" Foreground="{StaticResource text}" FontFamily="Amiri, Cairo, 'Segoe UI', Tahoma" 
                    VerticalAlignment="Center" Margin="40" />
            <Button Height="50" Background="Transparent" Width="150" BorderThickness="0" Style="{StaticResource Roundedbtn}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/img/show.png" Width="40" Height="35" />
                    <TextBlock Text="عرض" VerticalAlignment="Center" Style="{StaticResource TextBlockStyle}" FontSize="22"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <DataGrid  SelectionChanged="change_selected_record" AutoGeneratingColumn="change_header_name"  
                  FlowDirection="RightToLeft" IsReadOnly="True" FontSize="16" x:Name="data_grid"  Grid.Row="1" ColumnWidth="0.5*"/>
    </Grid>
</UserControl>
